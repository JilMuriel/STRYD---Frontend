# 08 — Testing Infrastructure

## Priority
🔴 High

## Current Issues
- No test framework setup
- No baseline test coverage

## Recommendation

Set up Vitest + Testing Library with React 19-compatible package versions.

## Actions
1. Add testing dependencies (latest compatible versions)
2. Add `vitest.config.ts` and setup file
3. Add `renderWithProviders` utility
4. Add smoke tests for key routes/components
